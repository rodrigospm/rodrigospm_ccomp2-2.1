#ifndef _POINT_H_
#define _POINT_H_

template <typename T>

class Point {
    private:
        T x, y;
    public:
        Point(const T u = 0, const T v = 0) : x(u), y(v) {}
        T.getX() const;
        T.getY() const;

};

template <typename T> 
T getX(const T x) {
    return u;
}

template <typename T> 
T getY(const T y) {
    return v;
}

#endif